# Copy to .env for local development:
#   cp .env.example .env
#
# Must match server MESSAGES_VIEW_KEY.
# Note: VITE_ values are bundled into the client build — use a strong unique key,
# and treat the inbox URL as private.

VITE_MESSAGES_VIEW_KEY=change-me-to-a-secret
